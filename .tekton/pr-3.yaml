---
apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: "plr-3"
  annotations:
    pipelinesascode.tekton.dev/on-cel-expression: | 
      (event == "push" || event == "pull_request") && (target_branch == "main" || target_branch == "test-9132")
spec:
  pipelineSpec:
    tasks:
      - name: task
        taskSpec:
          steps:
            - name: extract-commit-hash
              image: registry.access.redhat.com/ubi9/ubi
              script: |
                sleep 20s
                exit 0
